Most parts of the practical went well: I successfully cloned the GitHub repository to my local computer, edited Introductions.txt to replace Rob's information with my own, and copied thingsIlike.txt to the Practical3 directory. Committing local changes with git add and git commit also worked as expected.
The most challenging part was pushing local changes to GitHub (git push origin master). I initially encountered permission denied errors, and later realized I needed to use a GitHub Personal Access Token instead of a password. Additionally, I made mistakes with file paths (e.g., adding "Practical3/" prefix when already in the Practical3 directory), which caused git add to fail. Copying files also unexpectedly overwrote existing files because the cp command was set to force overwrite in my Cygwin environment.